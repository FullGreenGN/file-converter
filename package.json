{
  "name": "@fullgreen/converter",
  "version": "1.0.0",
  "description": "Professional CLI to convert HEIC and DOCX files.",
  "bin": {
    "fullgreen-convert": "dist/index.js"
  },
  "type": "module",
  "main": "dist/index.js",
  "engines": {
    "node": ">=20 <25"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "pnpm build && node --test test/smoke.test.mjs",
    "prepare": "pnpm run build"
  },
  "dependencies": {
    "chalk": "^5.6.2",
    "commander": "^14.0.3",
    "inquirer": "^13.4.1",
    "libreoffice-convert": "^1.8.1",
    "ora": "^9.3.0",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "tsup": "^8.5.1",
    "typescript": "^5.5.3"
  },
  "publishConfig": {
    "access": "public"
  }
}
